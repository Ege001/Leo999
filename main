--[[ 
    👑 SOVEREIGN V46 | BROOKHAVEN ULTIMATE FIX
    OWNER: EgeKazak_Real1
    STATUS: FULL INTEGRATED & UI FIXED
]]

-- 1. BAŞLATICI VE SERVİSLER
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local lp = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- GÜVENLİ YÜKLEME EKRANI [cite: 1, 2]
local function SafeLoading()
    local sg = Instance.new("ScreenGui", game.CoreGui)
    local frame = Instance.new("Frame", sg)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundColor3 = Color3.fromRGB(5, 5, 10)
    local txt = Instance.new("TextLabel", frame)
    txt.Size = UDim2.new(0, 500, 0, 100)
    txt.Position = UDim2.new(0.5, -250, 0.45, 0)
    txt.Text = "SOVEREIGN APEX V46\nBrookhaven Sistemleri Hazırlanıyor..."
    txt.TextColor3 = Color3.fromRGB(0, 170, 255)
    txt.Font = Enum.Font.GothamBold
    txt.TextSize = 30
    txt.BackgroundTransparency = 1
    task.wait(1.5)
    sg:Destroy()
end
pcall(SafeLoading)

-- 2. RAYFIELD UI YÜKLEME (GÜNCEL LİNK)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- 3. GLOBAL DEĞİŞKENLER [cite: 3]
_G.Speed = 16
_G.Jump = 50
_G.FOV = 70
_G.Noclip = false
_G.InfJump = false

-- 4. ANA PENCERE OLUŞTURMA 
local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN V46 | BROOKHAVEN",
   LoadingTitle = "EgeKazak_Real1 Özel Sürüm",
   LoadingSubtitle = "Full Entegre Sistem",
   Theme = "DarkBlue",
   ConfigurationSaving = { Enabled = false }
})

-- SEKMELER [cite: 6]
local MainTab = Window:CreateTab("🏠 Ana Kontrol", 4483362458)
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)
local TPTab = Window:CreateTab("✈️ Konumlar", 4483362458)
local ItemTab = Window:CreateTab("📦 Eşyalar", 4483362458)
local ViewerTab = Window:CreateTab("🔭 İzleyici", 4483362458)

-- 5. HAREKET KONTROLLERİ [cite: 7, 8, 10]
MoveTab:CreateSlider({
   Name = "Yürüme Hızı",
   Range = {16, 500},
   CurrentValue = _G.Speed,
   Callback = function(v) _G.Speed = v end,
})

MoveTab:CreateSlider({
   Name = "Zıplama Gücü",
   Range = {50, 500},
   CurrentValue = _G.Jump,
   Callback = function(v) _G.Jump = v end,
})

MoveTab:CreateToggle({
   Name = "Noclip (Duvar Geç)",
   Callback = function(v) _G.Noclip = v end,
})

MoveTab:CreateToggle({
   Name = "Sonsuz Zıplama",
   Callback = function(v) _G.InfJump = v end,
})

-- 6. BROOKHAVEN KONUMLAR (IŞINLANMA) 
local locs = {
    ["Banka Kasası"] = CFrame.new(-10.73, 15.45, 275.05),
    ["Polis Merkezi"] = CFrame.new(-119.57, 2.72, 12.24),
    ["Gizli Göl Evi"] = CFrame.new(-485.6, 23.5, 680.9),
    ["Yat"] = CFrame.new(-380.1, 15.2, -420.5),
    ["Gizli Çatı Katı"] = CFrame.new(-150.2, 55.4, 180.3),
    ["Hastane"] = CFrame.new(-150.0, 3.0, 50.0),
    ["Oyun Salonu"] = CFrame.new(-200.0, 3.0, 100.0)
}

for name, cf in pairs(locs) do
    TPTab:CreateButton({
        Name = name,
        Callback = function()
            if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                lp.Character.HumanoidRootPart.CFrame = cf
            end
        end
    })
end

-- 7. EŞYA SİSTEMİ 
ItemTab:CreateButton({
    Name = "💵 Money Gun Al",
    Callback = function()
        pcall(function()
            local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true)
            if remote then remote:FireServer("MoneyGun") end
        end)
    end
})

ItemTab:CreateButton({
    Name = "🔑 Tüm Anahtarları Al",
    Callback = function()
        local keys = {"HouseKey", "CarKey", "SafeKey"}
        for _, k in pairs(keys) do
            pcall(function()
                local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true)
                if remote then remote:FireServer(k) end
            end)
        end
    end
})

-- 8. AVATAR VE İZLEME [cite: 34, 40]
local function getNames()
    local t = {}
    for _,p in ipairs(Players:GetPlayers()) do table.insert(t, p.Name) end
    return t
end

local selectedPlayer = lp.Name
local dropdown = ViewerTab:CreateDropdown({
    Name = "Oyuncu Seç",
    Options = getNames(),
    CurrentOption = selectedPlayer,
    Callback = function(opt) selectedPlayer = opt end,
})

ViewerTab:CreateButton({
    Name = "🔍 İzle",
    Callback = function()
        local target = Players:FindFirstChild(selectedPlayer)
        if target and target.Character then
            Camera.CameraSubject = target.Character:FindFirstChildOfClass("Humanoid")
        end
    end
})

ViewerTab:CreateButton({
    Name = "🧍‍♂️ Avatar Kopyala",
    Callback = function()
        local p = Players:FindFirstChild(selectedPlayer)
        if p and lp.Character then
            local desc = Players:GetHumanoidDescriptionFromUserId(p.UserId)
            lp.Character:FindFirstChildOfClass("Humanoid"):ApplyDescription(desc)
        end
    end
})

-- 9. SİSTEM DÖNGÜSÜ (GÜNCELLEME) 
RunService.Heartbeat:Connect(function()
    pcall(function()
        if lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") then
            local h = lp.Character:FindFirstChildOfClass("Humanoid")
            h.WalkSpeed = _G.Speed
            h.JumpPower = _G.Jump
            h.UseJumpPower = true
        end
        if _G.Noclip and lp.Character then
            for _,part in pairs(lp.Character:GetDescendants()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)
end)

-- SON BİLDİRİM [cite: 53]
Rayfield:Notify({
    Title = "Sovereign V46 Aktif",
    Content = "Brookhaven Full ve UI Fix Sürümü Yüklendi!",
    Duration = 5
})
