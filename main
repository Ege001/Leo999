--[[ 
    👑 SOVEREIGN V46 | BROOKHAVEN ULTIMATE EDITION
    OWNER: EgeKazak_Real1
    STATUS: FULL STABLE & ALL FEATURES ENABLED
]]

-- 1. BAŞLATICI VE ANA DEĞİŞKENLER
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local lp = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- Güvenli Global Değerler
getgenv().Speed = 16
getgenv().Jump = 50
getgenv().Noclip = false
getgenv().InfJump = false
getgenv().AdsMessage = "SOVEREIGN V46"

-- 2. RAYFIELD UI KÜTÜPHANESİ (FIXED LOAD)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- 3. ANA PENCERE
local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN V46 | BROOKHAVEN",
   LoadingTitle = "EgeKazak_Real1 Dashboard",
   LoadingSubtitle = "Full Entegre & Ads System",
   Theme = "DarkBlue",
   ConfigurationSaving = { Enabled = false }
})

-- SEKMELER
local DashTab = Window:CreateTab("📊 Dashboard", 4483362458)
local AdsTab = Window:CreateTab("📢 Ads & Panolar", 4483362458)
local ItemTab = Window:CreateTab("📦 Eşyalar", 4483362458)
local TPTab = Window:CreateTab("✈️ Konumlar", 4483362458)
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)

-- 4. DASHBOARD (ANA SAYFA ÖZELLİKLERİ)
DashTab:CreateSection("Sunucu Durumu")
DashTab:CreateLabel("Kullanıcı: " .. lp.Name)
DashTab:CreateLabel("ID: " .. lp.UserId)

DashTab:CreateButton({
    Name = "🛠️ Tüm Eventleri Tetikle (Trigger All)",
    Callback = function()
        for _,v in pairs(game:GetDescendants()) do
            if v:IsA("RemoteEvent") and (v.Name:lower():find("click") or v.Name:lower():find("trigger")) then
                v:FireServer()
            end
        end
        Rayfield:Notify({Title="Başarılı", Content="Tüm RemoteEvent'ler zorlandı!", Duration=3})
    end
})

DashTab:CreateButton({
    Name = "🏠 Başlangıca Işınlan (Spawn)",
    Callback = function()
        if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
            lp.Character.HumanoidRootPart.CFrame = CFrame.new(-160, 3, 20)
        end
    end
})

-- 5. ADS MENU & PANO MANİPÜLASYONU
AdsTab:CreateSection("Reklam Pano Kontrolü")

AdsTab:CreateInput({
   Name = "Pano Metni",
   PlaceholderText = "Yazı yazın...",
   Callback = function(val) getgenv().AdsMessage = val end,
})

AdsTab:CreateButton({
   Name = "📢 Reklamı Yayınla (Update Ads)",
   Callback = function()
       pcall(function()
           -- Brookhaven'ın farklı Remote isimlerini tarar
           local adsRemote = ReplicatedStorage:FindFirstChild("UpdateAds", true) 
               or ReplicatedStorage:FindFirstChild("AdsRemote", true)
               or ReplicatedStorage:FindFirstChild("AdsEvent", true)
           
           if adsRemote then
               adsRemote:FireServer(getgenv().AdsMessage)
               Rayfield:Notify({Title="Ads", Content="Panolar güncellendi!", Duration=3})
           else
               Rayfield:Notify({Title="Hata", Content="Pano sistemi bulunamadı!", Duration=3})
           end
       end)
   end,
})

-- 6. EŞYA SİSTEMİ (BROOKHAVEN SPECIAL)
ItemTab:CreateSection("Tool & Envanter")

ItemTab:CreateButton({
    Name = "💵 Money Gun (Para Silahı)",
    Callback = function()
        pcall(function()
            local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true)
            if remote then remote:FireServer("MoneyGun") end
        end)
    end
})

ItemTab:CreateButton({
    Name = "🔑 Tüm Anahtarlar (Keys)",
    Callback = function()
        local keys = {"HouseKey", "CarKey", "SafeKey", "KeyCard"}
        local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true)
        if remote then
            for _, k in pairs(keys) do remote:FireServer(k) end
        end
    end
})

ItemTab:CreateButton({
    Name = "🛡️ Polis Ekipmanları",
    Callback = function()
        local remote = ReplicatedStorage:FindFirstChild("ItemRemote", true)
        if remote then 
            remote:FireServer("Handcuffs")
            remote:FireServer("Taser")
        end
    end
})

-- 7. KONUMLAR (IŞINLANMA)
local locations = {
    ["Banka Kasası"] = CFrame.new(-10, 15, 275),
    ["Polis Karakolu"] = CFrame.new(-120, 3, 12),
    ["Gizli Göl Evi"] = CFrame.new(-485, 23, 680),
    ["Yat (Havuzlu)"] = CFrame.new(-380, 15, -420),
    ["Hastane"] = CFrame.new(-150, 3, 50),
    ["Uçak Pist"] = CFrame.new(-250, 20, -300)
}

for name, cf in pairs(locations) do
    TPTab:CreateButton({
        Name = "✈️ " .. name,
        Callback = function()
            if lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") then
                lp.Character.HumanoidRootPart.CFrame = cf
            end
        end
    })
end

-- 8. HAREKET KONTROLLERİ
MoveTab:CreateSlider({
   Name = "Yürüme Hızı",
   Range = {16, 500},
   CurrentValue = 16,
   Callback = function(v) getgenv().Speed = v end,
})

MoveTab:CreateSlider({
   Name = "Zıplama Gücü",
   Range = {50, 500},
   CurrentValue = 50,
   Callback = function(v) getgenv().Jump = v end,
})

MoveTab:CreateToggle({
   Name = "Noclip (Duvar Geçme)",
   Callback = function(v) getgenv().Noclip = v end,
})

MoveTab:CreateToggle({
   Name = "Sonsuz Zıplama",
   Callback = function(v) getgenv().InfJump = v end,
})

-- 9. SİSTEM DÖNGÜSÜ (BACKEND)
RunService.Heartbeat:Connect(function()
    pcall(function()
        if lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") then
            local hum = lp.Character:FindFirstChildOfClass("Humanoid")
            hum.WalkSpeed = getgenv().Speed
            hum.JumpPower = getgenv().Jump
            hum.UseJumpPower = true
            
            if getgenv().Noclip then
                for _, part in pairs(lp.Character:GetDescendants()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end
    end)
end)

UserInputService.JumpRequest:Connect(function()
    if getgenv().InfJump and lp.Character and lp.Character:FindFirstChildOfClass("Humanoid") then
        lp.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- BİTİŞ BİLDİRİMİ
Rayfield:Notify({
    Title = "Sovereign V46 Yüklendi",
    Content = "EgeKazak_Real1 - Keyifli Oyunlar!",
    Duration = 5
})
