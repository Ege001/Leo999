--[[
    👑 SOVEREIGN V46 | APEX ULTIMATE
    OWNER: EgeKazak_Real1
    LOG: Decompiled Data (ShowGamepass/Gamepass) sızması tamamlandı.
]]

-- 🌀 GÜVENLİ LOADING SCREEN
local function SafeLoading()
    local sg = Instance.new("ScreenGui", game.CoreGui)
    local frame = Instance.new("Frame", sg)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundColor3 = Color3.fromRGB(5, 5, 10)
    
    local txt = Instance.new("TextLabel", frame)
    txt.Size = UDim2.new(0, 500, 0, 100)
    txt.Position = UDim2.new(0.5, -250, 0.45, 0)
    txt.Text = "SOVEREIGN APEX V46\nSistemler Zorlanıyor..."
    txt.TextColor3 = Color3.fromRGB(0, 170, 255)
    txt.Font = Enum.Font.GothamBold
    txt.TextSize = 30
    txt.BackgroundTransparency = 1
    
    task.wait(2)
    sg:Destroy()
end
pcall(SafeLoading)

-- 👑 ANA MOTOR
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local lp = game.Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local runService = game:GetService("RunService")
local uis = game:GetService("UserInputService")

-- 🛠️ VIP & ADS MODÜL BYPASS (ShowGamepass.txt ve Gamepass.txt Verileri)
pcall(function()
    -- VIP Eşya Kontrolünü Kır 
    local sgp = require(rs.Modules.Client.UI.ShowGamepass)
    sgp.IsOwned = function() return true end
    sgp.IsPlayerOwned = function() return true end
    
    -- Reklam (Ads) Sistemini Kır [cite: 40, 42]
    local gpc = require(rs.Modules.Client.Data.Gamepass)
    gpc.CanPlayerViewAdvertisements = function() return true end
end)

local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN V46 | APEX",
   LoadingTitle = "EgeKazak_Real1 Özel Sürüm",
   LoadingSubtitle = "1000 Limit Unlocked",
   Theme = "DarkBlue",
   ConfigurationSaving = { Enabled = false }
})

-- DEĞİŞKENLER
_G.Speed = 16
_G.Jump = 50
_G.Noclip = false
_G.InfJump = false

-- --- [1. HAREKET (SPEED & JUMP FIX)] ---
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)

MoveTab:CreateSlider({
   Name = "Hız (WalkSpeed)",
   Range = {16, 1000}, -- İstediğin 1000 sınırı eklendi
   CurrentValue = 16,
   Callback = function(v) _G.Speed = v end,
})

MoveTab:CreateSlider({
   Name = "Zıplama (JumpPower)",
   Range = {50, 1000}, -- İstediğin 1000 sınırı eklendi
   CurrentValue = 50,
   Callback = function(v) _G.Jump = v end,
})

MoveTab:CreateToggle({
   Name = "Noclip (Duvar Geç)",
   Callback = function(v) _G.Noclip = v end,
})

MoveTab:CreateToggle({
   Name = "Sonsuz Zıplama",
   Callback = function(v) _G.InfJump = v end,
})

-- --- [2. ANA PANEL: ADS & ADMIN] ---
local MainTab = Window:CreateTab("🏠 Ana Kontrol", 4483362458)

MainTab:CreateButton({
   Name = "📢 REKLAM MENÜSÜNÜ ZORLA AÇ",
   Callback = function()
       pcall(function()
           local gui = lp.PlayerGui:FindFirstChild("MainGUIHandler", true)
           local ads = gui and gui:FindFirstChild("AdStore", true)
           if ads then ads.Visible = true end
       end)
   end,
})

MainTab:CreateButton({
   Name = "⌨️ CMDR ADMIN PANELİ (F2)",
   Callback = function()
       pcall(function()
           local cmdr = require(rs:WaitForChild("Packages"):WaitForChild("CmdrClient"))
           cmdr:Show()
       end)
   end,
})

-- --- [3. IŞINLANMA (GÜNCEL KONUMLAR)] ---
local TPTab = Window:CreateTab("✈️ Işınlanma", 4483362458)

TPTab:CreateButton({
    Name = "💰 BANKA (YENİ KONUM FIX)",
    Callback = function()
        lp.Character.HumanoidRootPart.CFrame = CFrame.new(-10.7334747, 15.4523478, 275.049194)
    end,
})

TPTab:CreateButton({
    Name = "🏢 POLİS MERKEZİ",
    Callback = function()
        lp.Character.HumanoidRootPart.CFrame = CFrame.new(-119.57766, 2.72700167, 12.2390404)
    end,
})

-- --- [4. EŞYA SİSTEMİ: MONEY GUN] ---
local ToolTab = Window:CreateTab("📦 Eşyalar", 4483362458)

ToolTab:CreateButton({
   Name = "💵 MONEY GUN (VIP) AL",
   Callback = function()
       pcall(function()
           local remote = rs:FindFirstChild("ItemRemote", true)
           if remote then remote:FireServer("MoneyGun") end
           local tool = rs:FindFirstChild("MoneyGun", true)
           if tool then tool:Clone().Parent = lp.Backpack end
       end)
   end,
})

-- --- [ DÖNGÜSEL SİSTEMLER - KESİN ÇÖZÜM ] ---
runService.Heartbeat:Connect(function()
    pcall(function()
        if lp.Character and lp.Character:FindFirstChild("Humanoid") then
            -- Oyunun değerleri 16/50'ye çekmesini engellemek için sürekli zorla
            lp.Character.Humanoid.WalkSpeed = _G.Speed
            lp.Character.Humanoid.JumpPower = _G.Jump
            lp.Character.Humanoid.UseJumpPower = true
        end
        if _G.Noclip and lp.Character then
            for _, v in pairs(lp.Character:GetDescendants()) do
                if v:IsA("BasePart") then v.CanCollide = false end
            end
        end
    end)
end)

uis.JumpRequest:Connect(function()
    if _G.InfJump and lp.Character then
        lp.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)
