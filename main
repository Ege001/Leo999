--[[
    👑 SOVEREIGN V42 | APEX EDITION
    OWNER: EgeKazak_Real1
    LOG: Decompiled Data (ShowGamepass/Gamepass) Integrated
]]

-- 🌀 1. PRO LOADING SCREEN
local function StartLoading()
    local sg = Instance.new("ScreenGui", game.CoreGui)
    local frame = Instance.new("Frame", sg)
    frame.Size = UDim2.new(1, 0, 1, 0)
    frame.BackgroundColor3 = Color3.fromRGB(5, 5, 10)
    
    local txt = Instance.new("TextLabel", frame)
    txt.Size = UDim2.new(0, 600, 0, 100)
    txt.Position = UDim2.new(0.5, -300, 0.4, 0)
    txt.Text = "SOVEREIGN APEX V42\nEge İçin VIP Sistemler Kırılıyor..."
    txt.TextColor3 = Color3.fromRGB(0, 170, 255)
    txt.Font = Enum.Font.GothamBold
    txt.TextSize = 35
    txt.BackgroundTransparency = 1
    
    task.wait(3)
    sg:Destroy()
end
StartLoading()

-- 👑 2. ANA MOTOR VE MODÜL SIZMASI (VIP & ADS BYPASS)
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
local lp = game.Players.LocalPlayer
local rs = game:GetService("ReplicatedStorage")
local runService = game:GetService("RunService")
local uis = game:GetService("UserInputService")

-- Gönderdiğin dosyalardaki mantığı kandırıyoruz
pcall(function()
    local sgp = require(rs.Modules.Client.UI.ShowGamepass)
    local gpc = require(rs.Modules.Client.Data.Gamepass)
    
    -- VIP Eşya Kontrolünü Kır
    sgp.IsOwned = function() return true end
    
    -- Reklam (Ads) Sistemini Kır
    gpc.CanPlayerViewAdvertisements = function() return true end
    gpc.GetAdvertisementsEnabled = function() return true end
end)

local Window = Rayfield:CreateWindow({
   Name = "👑 SOVEREIGN V42 | APEX",
   LoadingTitle = "Hoş Geldin Owner Ege",
   LoadingSubtitle = "Full System Unlocked",
   Theme = "DarkBlue",
   ConfigurationSaving = { Enabled = true, FolderName = "SovereignApex" }
})

-- DEĞİŞKENLER
_G.Speed = 16
_G.Jump = 50
_G.InfJump = false
_G.Noclip = false
_G.Fly = false
_G.Gravity = workspace.Gravity

-- --- [1. ANA PANEL: ADS & CMDR & ADMIN] ---
local MainTab = Window:CreateTab("🏠 Ana Kontrol", 4483362458)

MainTab:CreateButton({
   Name = "📢 REKLAM & SPONSOR MENÜSÜNÜ ZORLA",
   Callback = function()
       pcall(function()
           local handler = lp.PlayerGui:FindFirstChild("MainGUIHandler", true)
           if handler then
               local adStore = handler:FindFirstChild("AdStore", true)
               if adStore then 
                   adStore.Visible = true 
                   adStore.Position = UDim2.new(0.5, 0, 0.5, 0)
               end
           end
           Rayfield:Notify({Title = "ADS", Content = "Reklam sistemi tetiklendi!", Duration = 3})
       end)
   end,
})

MainTab:CreateButton({
   Name = "⌨️ CMDR ADMIN PANELİNİ AÇ",
   Callback = function()
       pcall(function()
           local Cmdr = require(rs:WaitForChild("Packages"):WaitForChild("CmdrClient"))
           Cmdr:Show()
           Rayfield:Notify({Title = "CMDR", Content = "Admin Paneli Aktif! (F2/Quote)", Duration = 3})
       end)
   end,
})

-- --- [2. FİZİK & HAREKET (SPEED, JUMP, FLY...)] ---
local MoveTab = Window:CreateTab("⚡ Hareket", 4483362458)

MoveTab:CreateSlider({
   Name = "Hız (Speed)", Range = {16, 500}, CurrentValue = 16,
   Callback = function(v) _G.Speed = v end,
})

MoveTab:CreateSlider({
   Name = "Zıplama (Jump)", Range = {50, 500}, CurrentValue = 50,
   Callback = function(v) _G.Jump = v end,
})

MoveTab:CreateSlider({
   Name = "Yerçekimi (Gravity)", Range = {0, 196}, CurrentValue = 196,
   Callback = function(v) workspace.Gravity = v end,
})

MoveTab:CreateToggle({
   Name = "Sonsuz Zıplama (Inf Jump)",
   Callback = function(v) _G.InfJump = v end,
})

MoveTab:CreateToggle({
   Name = "Duvar Geçme (Noclip)",
   Callback = function(v) _G.Noclip = v end,
})

-- --- [3. IŞINLANMA (GÜNCEL NOKTALARIN)] ---
local TPTab = Window:CreateTab("✈️ Işınlanma", 4483362458)

TPTab:CreateButton({
    Name = "💰 BANKA (YENİ KONUM)",
    Callback = function()
        lp.Character.HumanoidRootPart.CFrame = CFrame.new(-10.7334747, 15.4523478, 275.049194)
    end,
})

TPTab:CreateButton({
    Name = "🏢 POLİS MERKEZİ",
    Callback = function()
        lp.Character.HumanoidRootPart.CFrame = CFrame.new(-119.57766, 2.72700167, 12.2390404)
    end,
})

-- --- [4. EŞYA SİSTEMİ: MONEY GUN] ---
local ToolTab = Window:CreateTab("📦 Eşyalar", 4483362458)

ToolTab:CreateButton({
   Name = "💵 MONEY GUN (VIP) GETİR",
   Callback = function()
       pcall(function()
           local remote = rs:FindFirstChild("ItemRemote", true)
           if remote then remote:FireServer("MoneyGun") end
           local tool = rs:FindFirstChild("MoneyGun", true)
           if tool then tool:Clone().Parent = lp.Backpack end
       end)
   end,
})

-- --- [ DÖNGÜSEL SİSTEMLER (TICK) ] ---
runService.Heartbeat:Connect(function()
    if lp.Character and lp.Character:FindFirstChild("Humanoid") then
        lp.Character.Humanoid.WalkSpeed = _G.Speed
        lp.Character.Humanoid.JumpPower = _G.Jump
    end
    
    if _G.Noclip and lp.Character then
        for _, part in pairs(lp.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

uis.JumpRequest:Connect(function()
    if _G.InfJump and lp.Character then
        lp.Character.Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- --- [ AYARLAR ] ---
local SettingsTab = Window:CreateTab("⚙️ Ayarlar", 4483362458)
SettingsTab:CreateButton({Name = "🔴 SCRIPT'İ YOK ET (Destroy)", Callback = function() Rayfield:Destroy() end})
